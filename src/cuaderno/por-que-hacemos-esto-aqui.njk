---
layout: layouts/article.njk
title: "Por qué hacemos esto aquí — Cuaderno de campo — Cavero Diego"
description: "Podríamos haber elegido otra estética. Elegimos Castilla."
section: campo
langBtn: false
articleDate: "2025-05-20"
pageStyle: |
  /* Nav background comes from global.css */

  .entrada-hero {
    min-height: 65vh;
    background: #2a3229;
    display: flex; align-items: flex-end;
    padding: 0 8vw 7vh;
    position: relative;
    overflow: hidden;
  }
  .entrada-hero img {
    position: absolute; inset: 0;
    width: 100%; height: 100%; object-fit: cover;
    display: block;
  }
  .entrada-hero-overlay {
    position: absolute; inset: 0;
    background: linear-gradient(to top, rgba(42,50,41,.75) 0%, rgba(42,50,41,.15) 60%);
  }
  .entrada-hero-content {
    position: relative; z-index: 1;
    max-width: 760px;
  }
  .entrada-hero-meta {
    display: flex; align-items: center; gap: 1.4rem;
    margin-bottom: 1.4rem;
  }
  .entrada-cat {
    font-size: .58rem; letter-spacing: .28em;
    text-transform: uppercase; color: var(--blue);
  }
  .entrada-fecha {
    font-size: .58rem; letter-spacing: .18em;
    text-transform: uppercase; color: rgba(232,229,220,.5);
  }
  .entrada-hero-titulo {
    font-family: 'Boska', serif;
    font-size: clamp(2rem, 5vw, 5rem);
    font-weight: 400; line-height: 1.05; letter-spacing: -.02em;
    color: var(--cream);
  }

  .entrada-layout {
    display: grid;
    grid-template-columns: 1fr min(680px, 60vw) 1fr;
    padding: 10vh 0;
  }
  .entrada-content { grid-column: 2; }

  .entrada-content p {
    font-size: 1rem;
    line-height: 1.9;
    color: var(--ink);
    margin-bottom: 1.8rem;
  }
  .entrada-content h2 {
    font-family: 'Boska', serif;
    font-size: clamp(1.4rem, 2.8vw, 2.2rem);
    font-weight: 400; line-height: 1.1; letter-spacing: -.01em;
    margin: 3.6rem 0 1.4rem;
  }
  .entrada-content blockquote {
    margin: 3rem 0;
    padding-left: 2rem;
    border-left: 2px solid var(--ink-10);
    font-family: 'Boska', serif;
    font-size: clamp(1.1rem, 2.2vw, 1.6rem);
    font-style: italic;
    color: var(--stone);
    line-height: 1.4;
  }
  .entrada-content .imagen-entrada {
    width: 100%;
    aspect-ratio: 3/2;
    background: #4a5438;
    border-radius: 2px;
    margin: 3rem 0;
    overflow: hidden;
  }
  .entrada-content .imagen-entrada img {
    width: 100%; height: 100%; object-fit: cover; display: block;
  }
  .imagen-caption {
    font-size: .68rem; letter-spacing: .14em;
    text-transform: uppercase; color: var(--stone);
    margin-top: -.6rem; margin-bottom: 2rem;
  }

  .entrada-nav {
    padding: 6vh 8vw;
    border-top: 1px solid var(--ink-10);
    display: flex; align-items: center; justify-content: space-between;
  }
  .volver-link {
    display: inline-flex; align-items: center; gap: .6rem;
    font-size: .66rem; letter-spacing: .2em; text-transform: uppercase;
    color: var(--stone); text-decoration: none; transition: color .3s;
  }
  .volver-link svg { transition: transform .3s; }
  .volver-link:hover { color: var(--ink); }
  .volver-link:hover svg { transform: translateX(-4px); }

  .siguiente-link {
    display: flex; flex-direction: column; align-items: flex-end;
    text-decoration: none; color: var(--ink); gap: .4rem;
  }
  .siguiente-label {
    font-size: .58rem; letter-spacing: .22em;
    text-transform: uppercase; color: var(--stone);
  }
  .siguiente-titulo {
    font-family: 'Boska', serif;
    font-size: clamp(1rem, 1.8vw, 1.4rem);
    font-weight: 400; line-height: 1.2;
    text-align: right; max-width: 28ch;
  }

  @media (max-width: 900px) {
    .entrada-layout { grid-template-columns: 1fr; padding: 8vh 8vw; }
    .entrada-content { grid-column: 1; }
  }
  @media (max-width: 560px) {
    .entrada-nav { flex-direction: column; align-items: flex-start; gap: 2.4rem; }
    .siguiente-link { align-items: flex-start; }
    .siguiente-titulo { text-align: left; }
  }
---

<!-- ─── HERO ─────────────────────────────────────────────────────── -->
<div class="entrada-hero">
  <div class="entrada-hero-overlay"></div>
  <div class="entrada-hero-content">
    <div class="entrada-hero-meta">
      <span class="entrada-cat">Reflexión</span>
      <span class="entrada-fecha">20 Mayo 2025</span>
    </div>
    <h1 class="entrada-hero-titulo">
      Por qué hacemos<br>esto aquí
    </h1>
  </div>
</div>

<!-- ─── CONTENIDO ────────────────────────────────────────────────── -->
<article class="entrada-layout">
  <div class="entrada-content">

    <p>
      Podríamos haber elegido otra estética. Podríamos haber dicho que venimos del norte,
      del mediterráneo, de ningún sitio en particular. Haber construido algo que no fuera
      de ningún lugar, que pudiera venderse igual en Berlín que en Tokio sin necesidad
      de explicar nada. Elegimos lo contrario.
    </p>

    <p>
      Elegimos Castilla. Y conviene explicar por qué, porque no fue una decisión romántica
      ni una estrategia de diferenciación. Fue la única opción honesta.
    </p>

    <h2>El territorio como argumento</h2>

    <p>
      Castilla-La Mancha tiene una reputación compleja. En España, el nombre evoca
      una mezcla de orgullo difuso y distancia cultural. Es la tierra del Quijote,
      sí, pero también la tierra que la gente atraviesa de camino a otro sitio.
      Eso nos interesó desde el principio: el potencial de un territorio que todavía
      no ha sido agotado por el marketing.
    </p>

    <p>
      Lo que tiene Castilla es una materialidad intacta. Una luz que no tiene
      que competir con el mar. Un silencio que no necesita explicarse.
      Una forma de estar en el paisaje que no es pintoresca sino funcional,
      casi mineral. Eso no se inventa. O viene del lugar o no viene de ningún sitio.
    </p>

    <blockquote>
      No buscábamos un escenario. Buscábamos una razón.
      Castilla es la razón.
    </blockquote>

    <h2>Por qué aquí y no en otro sitio</h2>

    <p>
      Hay marcas de lujo que fabrican en el mismo territorio que invocan.
      Hay muchas más que no. La distancia entre el origen declarado y el origen real
      es uno de los grandes fraudes silenciosos del sector. Nos negamos a esa fórmula.
    </p>

    <p>
      Cuando decimos Submeseta Sur, lo decimos porque ahí están los viñedos de los que
      viene la semilla del Sustrato 01. Ahí están los encares que dan forma al olor
      que queremos en nuestras velas. Ahí está el apicultor con el que trabajamos,
      las colmenas que producen la cera, el polvo fino de la estepa que se adhiere
      a todo lo que sale de allí. El territorio no es un argumento de marketing.
      Es un ingrediente.
    </p>

    <div class="imagen-entrada">
      <!-- Placeholder — sustituir por imagen real -->
    </div>
    <p class="imagen-caption">La estepa en diciembre, Castilla-La Mancha</p>

    <h2>La pregunta que nos hemos hecho</h2>

    <p>
      Cuando empezamos a construir esto, la pregunta que nos hacíamos constantemente era:
      ¿qué hace que algo sea de un sitio de verdad? No en el sentido geográfico
      o legal, sino en el sentido profundo. ¿Qué tiene que ocurrir para que un objeto
      lleve consigo el carácter de un lugar?
    </p>

    <p>
      La respuesta que encontramos fue sencilla: que las decisiones que lo forman
      nazcan de ese lugar. No solo las materias primas. La proporción, el olor,
      el peso, el silencio que hay en el envase cuando no hay nada que decorar.
      Todo eso es Castilla. No porque lo hayamos decidido así, sino porque no
      podemos hacer las cosas de otra manera viniendo de donde venimos.
    </p>

    <h2>Lo que significa hacer esto aquí</h2>

    <p>
      Hacer esto en Castilla tiene un coste que vale la pena pagar.
      Las distancias son largas. Los proveedores locales no siempre tienen
      la capacidad que necesitamos. La logística es más cara que si estuviéramos
      en Madrid o en Barcelona. Pero lo que ganamos es algo que no podemos comprar
      en otro sitio: la coherencia.
    </p>

    <p>
      Cavero Diego no puede existir sin el territorio que lo funda.
      No es una elección estética. Es la condición de posibilidad de todo lo demás.
      Cambia el territorio y cambia la marca. No hay versión de esto en otro lugar.
    </p>

  </div>
</article>

<!-- ─── NAV ENTRE ENTRADAS ───────────────────────────────────────── -->
<div class="entrada-nav">
  <a href="/cuaderno/" class="volver-link">
    <svg width="14" height="14" viewBox="0 0 16 16" fill="none">
      <path d="M13 8H3M7 4l-4 4 4 4" stroke="currentColor" stroke-width="1" stroke-linecap="round"/>
    </svg>
    Cuaderno de campo
  </a>
  <a href="/cuaderno/la-cera-de-abeja/" class="siguiente-link">
    <span class="siguiente-label">Siguiente entrada</span>
    <span class="siguiente-titulo">La cera de abeja y por qué no usamos otra cosa</span>
  </a>
</div>
